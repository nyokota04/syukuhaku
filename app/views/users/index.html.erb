<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<h1>ユーザー一覧</h1>
<% @users.each do |user| %>
  <%= link_to user_path(user) do %>
   <%= attachment_image_tag @user, :user_img, fallback: "no_image.png" %>
   <%= user.user_name %>
    <%= user.email %>
  <% end %>

<% end %>
